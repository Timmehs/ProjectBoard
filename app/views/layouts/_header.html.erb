<nav class="navbar">
    <h2>ProjectBoard</h2>
    <div class="auth-buttons right">
      <% if !current_user %>
        <button data-target="#myModal" data-toggle="modal" class="button button-primary">SIGN IN</button>
        <a href="#" class="button">SIGN UP</a>
      <% else %>
        <a data-method="delete" href="/api/sessions" rel="nofollow" class="btn btn-default">Logout</a>
      <% end %>
    </div>
</nav>
