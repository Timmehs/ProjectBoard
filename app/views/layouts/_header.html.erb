
<nav class="navbar">
    <a href="/#"><h1 class="brand">ProjectBoard</h1>
    <div class="auth-buttons right">
      <% if current_user %>
      <div class="user-avatar"
        style="background-image: url(<%= current_user.avatar %>)"></div>
        <a data-method="delete" href="/api/sessions" rel="nofollow"
					 class="btn btn-default logout">Logout</a>
       <a class="btn btn-default logout" href="#/projects/new">
         Add Project
       </a>
      <% else %>
        <a class="btn btn-primary" href="/auth/github/">Sign in with Github</a>
      <% end %>
    </div>
</nav>

<div class='hr'></div>